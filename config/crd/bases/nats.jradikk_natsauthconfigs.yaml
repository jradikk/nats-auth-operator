---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: natsauthconfigs.nats.jradikk
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
spec:
  group: nats.jradikk
  names:
    kind: NatsAuthConfig
    listKind: NatsAuthConfigList
    plural: natsauthconfigs
    singular: natsauthconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NatsAuthConfig is the Schema for the natsauthconfigs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NatsAuthConfigSpec defines the desired state of NatsAuthConfig
            properties:
              jwt:
                description: JWT configuration (required if mode is jwt or mixed)
                properties:
                  operatorName:
                    default: NATS Operator
                    description: OperatorName is the name of the NATS operator
                    type: string
                  operatorSeedSecret:
                    description: OperatorSeedSecret references an existing operator
                      seed (optional)
                    properties:
                      key:
                        default: operator.seed
                        description: Key within the Secret
                        type: string
                      name:
                        description: Name of the Secret containing the operator seed
                        type: string
                      namespace:
                        description: Namespace of the Secret
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  resolverDir:
                    default: /var/lib/nats-resolver
                    description: ResolverDir is the directory path where the resolver
                      is stored
                    type: string
                type: object
              mode:
                default: jwt
                description: Mode defines the authentication mode (token, jwt, or
                  mixed)
                enum:
                - token
                - jwt
                - mixed
                type: string
              natsURL:
                description: NatsURL is the URL for NATS clients to connect
                pattern: ^nats://.*
                type: string
              serverAuthConfig:
                description: ServerAuthConfig defines where to write the server auth
                  configuration
                properties:
                  key:
                    default: auth.conf
                    description: Key within the ConfigMap or Secret
                    type: string
                  name:
                    description: Name of the ConfigMap or Secret
                    type: string
                  namespace:
                    description: Namespace of the ConfigMap or Secret
                    type: string
                  type:
                    default: ConfigMap
                    description: Type of the resource (ConfigMap or Secret)
                    enum:
                    - ConfigMap
                    - Secret
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - mode
            - natsURL
            - serverAuthConfig
            type: object
          status:
            description: NatsAuthConfigStatus defines the observed state of NatsAuthConfig
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastReconciled:
                description: LastReconciled is the timestamp of the last reconciliation
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed NatsAuthConfig
                format: int64
                type: integer
              operatorPubKey:
                description: OperatorPubKey is the public key of the NATS operator
                  (JWT mode)
                type: string
              resolverReady:
                description: ResolverReady indicates if the resolver is ready (JWT
                  mode)
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
    additionalPrinterColumns:
    - name: Mode
      type: string
      jsonPath: .spec.mode
    - name: NATS URL
      type: string
      jsonPath: .spec.natsURL
    - name: Ready
      type: boolean
      jsonPath: .status.resolverReady
    - name: Age
      type: date
      jsonPath: .metadata.creationTimestamp
