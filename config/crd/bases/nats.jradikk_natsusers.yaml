---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: natsusers.nats.jradikk
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
spec:
  group: nats.jradikk
  names:
    kind: NatsUser
    listKind: NatsUserList
    plural: natsusers
    singular: natsuser
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NatsUser is the Schema for the natsusers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NatsUserSpec defines the desired state of NatsUser
            properties:
              accountRef:
                description: AccountRef references the NatsAccount (required for JWT
                  mode)
                properties:
                  name:
                    description: Name of the NatsAccount
                    type: string
                  namespace:
                    description: Namespace of the NatsAccount (defaults to same namespace)
                    type: string
                required:
                - name
                type: object
              authConfigRef:
                description: AuthConfigRef references the NatsAuthConfig
                properties:
                  name:
                    description: Name of the NatsAuthConfig
                    type: string
                  namespace:
                    description: Namespace of the NatsAuthConfig (defaults to same
                      namespace)
                    type: string
                required:
                - name
                type: object
              authType:
                default: inherit
                description: AuthType defines the authentication type (token, jwt,
                  or inherit from NatsAuthConfig)
                enum:
                - token
                - jwt
                - inherit
                type: string
              existingSeedSecret:
                description: ExistingSeedSecret references an existing user seed (optional,
                  JWT mode)
                properties:
                  name:
                    description: Name of the Secret
                    type: string
                  namespace:
                    description: Namespace of the Secret
                    type: string
                type: object
              passwordFrom:
                description: PasswordFrom defines how to obtain the password (for
                  token auth)
                properties:
                  generate:
                    description: Generate indicates whether to generate a random password
                    type: boolean
                  secretRef:
                    description: SecretRef references an existing Secret containing
                      the password
                    properties:
                      name:
                        description: Name of the Secret
                        type: string
                      namespace:
                        description: Namespace of the Secret
                        type: string
                    type: object
                type: object
              permissions:
                description: Permissions defines publish/subscribe permissions
                properties:
                  publishAllow:
                    description: PublishAllow is a list of subjects the user can publish
                      to
                    items:
                      type: string
                    type: array
                  publishDeny:
                    description: PublishDeny is a list of subjects the user cannot
                      publish to
                    items:
                      type: string
                    type: array
                  subscribeAllow:
                    description: SubscribeAllow is a list of subjects the user can
                      subscribe to
                    items:
                      type: string
                    type: array
                  subscribeDeny:
                    description: SubscribeDeny is a list of subjects the user cannot
                      subscribe to
                    items:
                      type: string
                    type: array
                type: object
              username:
                description: Username for token-based auth
                type: string
            required:
            - authConfigRef
            type: object
          status:
            description: NatsUserStatus defines the observed state of NatsUser
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastReconciled:
                description: LastReconciled is the timestamp of the last reconciliation
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed NatsUser
                format: int64
                type: integer
              publicKey:
                description: PublicKey is the public key of the user (JWT mode)
                type: string
              reason:
                description: Reason provides more detail about the current state
                type: string
              secretRef:
                description: SecretRef references the Secret containing user credentials
                properties:
                  name:
                    description: Name of the Secret
                    type: string
                  namespace:
                    description: Namespace of the Secret
                    type: string
                type: object
              state:
                default: Pending
                description: State represents the current state of the user
                enum:
                - Ready
                - Error
                - Pending
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
    additionalPrinterColumns:
    - name: Auth Type
      type: string
      jsonPath: .spec.authType
    - name: State
      type: string
      jsonPath: .status.state
    - name: Account
      type: string
      jsonPath: .spec.accountRef.name
    - name: Age
      type: date
      jsonPath: .metadata.creationTimestamp
